services:
  - type: web
    name: ai-xray
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false  # Hoặc set giá trị trực tiếp ở trang Render UI
